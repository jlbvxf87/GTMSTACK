# v0-original

The first static landing-page direction for GTMSTACK, preserved verbatim from the original `~/Desktop/GTM STACK/` folder. Single-page HTML/CSS/JS plus logos and storefront mocks.

This is the *starting point* the new creative iterates against — keep it untouched as a reference.

## Files

- `index.html` / `styles.css` / `script.js` — the landing page
- `*.png` — logos, hero, storefront mocks, landing-page mock
- `BACKEND_CONTRACT.md` — the backend spec that was sitting in `docs/`

## Viewing

Open `index.html` directly in a browser — no build step.
