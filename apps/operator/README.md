# apps/operator

`app.gtmstack.com` — operator dashboard and onboarding. Operators sign up, choose a template family (clinical / wellness / community), configure brand voice via AI, manage products, orders, subscriptions, customers, AI conversations, and analytics.

Auth context: **operator**. Reads/writes the core Supabase project. Never reads from clinical.
