# apps/storefront

Operator branded stores under custom domains and `*.gtmstack.shop`. One Next.js codebase that resolves brand + theme from the host header, then renders the seven universal sections with that brand's tokens.

Auth context: **customer**. Reads/writes the core Supabase project.

The Prime Wellness demo lives here: `primewellness.gtmstack.shop`.
